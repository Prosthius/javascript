/*
Arithmetic Operators
*/

+ //Adds two numbers together
- //Subtracts right number from left number
* //Multiplies two numbers together
/ //Divides left number by the right number
% //Returns remainder leftover after dividing the left number into the right in whole numbers
** //Equivalent to 'the power of'
(+ or - or * or * or / or % or **)= //Shorthand for e.g. x = x + 4


/*
Comparison Operators
*/
./comparison-operators.png
== and != //Do not consider datatype
=== and !== //Do consider datatype

"1" == 1; true
"1" === 1: false

/*
Number Operators
*/
toFixed() //Round a number to a fixed number of decimal places e.g:

let lotsOfDecimals = 1.29847294748;
let twoDecimals = lotsOfDecimals.toFixed(2);
twoDecimals;
"1.30"



typeof //Shows datatype
const //Sets unchangeable variable
let //Sets changeable variable
Number(variable) //To temporarily convert string into number
